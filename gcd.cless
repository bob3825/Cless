int LF;
/*!=
==
<=
>=
*/
int gcd (int a, int b)
{
	while (a != b) {
		if (a < b) {
			b = b -a;
		} else {
			a = a-b;
		}
	}
	return a;
}

int main ()
{
	int v1; int v2; int res;
	LF = 10;
	v1 = getint(); v2 = getint();
	res = gcd(v1,v2);
	putint(res); putchar(LF);
}
